# Copy this file to .env and fill in real values. Never commit .env itself.

DATABASE_URL=postgresql://user:password@localhost:5432/campuscert
JWT_SECRET=
JWT_EXPIRY_MINUTES=60

# AI-assisted question generation (Member 3 / backend/app/ai)
LLM_API_KEY=
LLM_API_URL=

# Sandbox/simulated payments only — no real gateway in the MVP
PAYMENTS_SANDBOX_MODE=true
